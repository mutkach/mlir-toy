var a = 20;
var z <2> = [1,2];
var y <2,3> = [[1,2],[2,3],[3,3]];

def multiply_transpose(a, b) {
  return transpose(a) * transpose(b);
}

def func(b) {
    var h = 2;
    var c = multiply_transpose(a, b);
}
